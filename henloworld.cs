using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class henloworld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
         //ihaveallcostumesonstrawberrycrepecookie
        print("cut cut perm perm cut cut");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
